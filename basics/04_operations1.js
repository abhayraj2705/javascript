let x=3
const y=x++ // y will remain constant and x will get incremented  postfix increments the values return it  before incrementing 

console.log(x,y);
let a=4
const b=++a // prefix first b is getting incremented and then obiously a is incrementing  prefix the incremented and it return the value after incrementing 

console.log(a,b);
